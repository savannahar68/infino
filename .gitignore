/target
lcov.info
*.profraw
.DS_Store

# Never commit secrets / local credentials.
.env
.env.*
*.env
*.pem
*.key

# Python bindings (built standalone with maturin; not a workspace member).
# Cargo.lock IS committed: the bindings are a built artifact (wheel) and
# CI builds them with `--locked` for deterministic, drift-proof deps.
infino-python/target/
infino-python/.venv/
infino-python/dist/
**/__pycache__/
**/.pytest_cache/
